# ForgeOS Workbook — Risk Register

Consolidated from `docs/business-rules.md`, `docs/workbook-dependency-map.md`,
`docs/workflow-map.md`, and `docs/input-output-map.md`. Severity is scored
against the ForgeOS migration goal (accurate, reproducible, auditable
estimating), not against the workbook continuing to be used as-is.

| # | Category | Finding | Evidence | Severity | Confidence |
|---|---|---|---|---|---|
| R1 | Formula inconsistencies | `PRICE OPTIONS` sheet: 1,068 formulas, effectively 100% of the sheet's logic, reference `'COST SUMMARY'!#REF!` — the sheet cannot compute anything. | business-rules.md Rule 7; 365 `#REF!` formulas workbook-wide, majority here | **High** | High (broken) / Low (why) |
| R2 | Hidden assumptions | `Base!B23 = 1.03` is labeled "Professional Services, 4% of Grand Total" — the value corresponds to 3%, not 4%. Unknown which is correct. | business-rules.md Rule 8 | **High** — silently over/under-charges every job using this factor | High (mismatch exists) / Low (which is right) |
| R3 | Hardcoded rates | 15 department labor rates hardcoded as literals on `COMPONENT 1` only, propagated to 43 sibling sheets via `=SUM('COMPONENT 1'!H<row>)`. No central rate table; no sheet protection on `COMPONENT 1`. | business-rules.md Rule 1 | **High** — single accidental overwrite corrupts every future estimate silently | High |
| R4 | Duplicated logic | Two independent, disconnected labor-rate systems: `LABOR RATES` (city table, zero formula references anywhere) vs. `COMPONENT 1`'s department table. No formula bridges them. | business-rules.md Rule 10 | **Medium** — unclear if `LABOR RATES` is dead data or a manual-lookup source; risk is using the wrong one as ForgeOS's seed data | Medium |
| R5 | Hardcoded rates | `Standard Cost Sheet!C15` embeds its derivation directly in a formula literal: `=(35*2)*1.25`, rather than referencing named rate cells. | business-rules.md Rule 9 | **Medium** — reasoning is lost if the value is ever copy-pasted elsewhere | High |
| R6 | Formula inconsistencies | `DATA` sheet's `Cities` table calculated column formula references a *different* table (`Cities13`) instead of its own — works only because the two tables are row-aligned by coincidence/copy-paste history. | business-rules.md Rule 11 | **Medium** — fragile; reordering either table desyncs tax rates silently | High |
| R7 | External links | Workbook links to a network-drive file (`Z-DRIVE ORLANDO-EAC/.../MASTER - (CLIENT,...).xlsm`) for 11 sheets of reference data; **9 of 11 cached sheets show `refreshError="1"`**. `TRUCKING & LOAD LIST` has a live formula (`=[1]TRUCKING & LOAD LIST!...`) depending on this broken link. | dependency-map.md §4 | **High** — any value traced through this link is unverifiable from the workbook alone, and is a hard Phase-1 import blocker until resolved | High |
| R8 | Workbook protection | Only 2 of 95 sheets (`SUPPLEMENTAL`, `PRICE OPTIONS`) have sheet protection enabled, and **neither has a password hash** — protection is a soft, trivially-bypassed guard, not real access control. 93 sheets are fully unprotected, including the master labor-rate table (R3). | workbook-inventory.md summary | **Medium** — expected for a single-user Excel file, but confirms ForgeOS must not assume any workbook-level access control carries meaning | High |
| R9 | Version control / estimate reproducibility | No version concept exists anywhere in the workbook — one mutable file represents "current state" with no history of prior rates, margins, or line items. `docProps/core.xml` only records a single last-modified author/timestamp for the whole file, not per-change. | workflow-map.md "Key finding"; data-model-v0.md `EstimateVersion`/`AuditEvent` | **High** — directly blocks ForgeOS goal of "estimate revisions, alternates, approvals" unless solved structurally in Phase 3 | High |
| R10 | Circular / volatile formulas | 84 volatile formulas found (`TODAY()` on ~1/sheet for date-stamp display, per `artifacts/formula_catalog.json` category `VOLATILE`). No self-referencing (single-cell circular) formulas detected by heuristic scan; no `iterativeCalculation` flag set in `calcPr`, consistent with no intentional circular design. **However**, the COMPONENT-family has 4,748 intra-family formula references whose *direction* was not exhaustively traced (dependency-map.md open question #2) — cannot fully rule out an accidental cross-sheet cycle without a live recalculation. | dependency-map.md; formula_catalog.json | **Low-Medium** — no confirmed circularity, but not exhaustively disproven either | Medium |
| R11 | Taxes and markups | Two separate, unreconciled markup/margin mechanisms exist: (a) `Price Summary`'s gross-up-by-margin formula (`cost/((100-margin%)/100)`), and (b) `PRICE OPTIONS`'s broken percentage-tier + `×1.075` system (R1). If (b) was ever meant to be the *real* pricing engine and (a) a simplified fallback, ForgeOS could port the wrong one. | business-rules.md Rules 6–7 | **High** — directly determines every job's realized price/margin | Medium |
| R12 | Unsupported Excel features | openpyxl emits explicit warnings on load: "Data Validation extension is not supported" (x14 list validations — 4 found, manually re-parsed via raw XML for this audit) and "wmf image format is not supported" / "DrawingML support is incomplete" (shapes/drawings dropped). Anything relying purely on openpyxl (without the raw-XML supplement built for this audit) would silently lose these. | xml_reader.py x14 handling; scan output | **Low** — mitigated in this audit's tooling, but a risk for any *other* tool naively using openpyxl alone | High |
| R13 | VBA behavior | Full VBA project decompiled (96 modules — see `artifacts/vba_inventory.json`). Only 2 modules contain any executable statement beyond boilerplate `Attribute` headers: `Sheet3` (mapped to `Start Page`) has a `Worksheet_SelectionChange` handler that shows/hides a floating "Calendar" shape near date-formatted cells. `Sheet4` (mapped to `SUPPLEMENTAL`) has no executable code at all. **No `Workbook_Open`, `Auto_Open`, or calculation-affecting macro exists anywhere.** | vba_reader.py extraction, decompiled via custom MS-OVBA implementation | **Low** — the one real macro is a pure UI convenience with zero business-logic impact; safe to drop entirely in ForgeOS (a native date picker replaces it) | High |
| R14 | Document fidelity | `PROPOSAL` (347 merged cells, print area) and other print-formatted sheets bake layout/branding directly into cell merges and print setup rather than a separable template. Reproducing visual fidelity in a web-based proposal generator (goal #3) is a design effort, not a data-migration one. | workbook-inventory.md; data-model-v0.md `ProposalTemplate` | **Medium** — scope/effort risk for Phase 4, not a data-correctness risk | Medium |
| R15 | Tenant data isolation | N/A in the source (single-company, single-user file) — flagged only because ForgeOS must decide multi-tenant scope before Phase 2 schema work; the workbook provides zero guidance here. | data-model-v0.md cross-cutting notes | **Medium** (project-planning risk, not a workbook defect) | N/A |
| R16 | Rounding | No workbook-wide rounding standard observed — spot-checked formulas (Rules 1–2, 6) do not wrap results in `ROUND()`; only 1 of 7 `ROUNDING`-category-tagged formulas found workbook-wide across 22,138 total (per `formula_catalog.json` category scan). Cent-level rounding behavior of a from-scratch ForgeOS engine may not match the workbook's floating-point-only results during Phase 1 comparison testing. | formula_catalog.json category distribution | **Low-Medium** — mainly a Phase 1 comparison-harness tolerance concern, not a business-logic defect | Medium |
| R17 | Named ranges / dead references | 3 of 10 workbook-scoped named ranges (`BoothNumber`, `Exhibitor`, `ShowName`) are `#REF!` errors with zero remaining consumers. | input-output-map.md §6 | **Low** — pure cleanup item, no functional impact, but signals the file has accumulated unrepaired structural damage over time (raises confidence that R1/R6 are part of a pattern, not isolated incidents) | High |
| R18 | Unresolved calculation duplication | `COST SUMMARY` appears to roll up costs via two separate mechanisms — a category-row block (confirmed, Rule 5) and a COMPONENT-family-referencing block (965 formulas, located but not cell-traced). If both represent the same underlying cost, a naive port would double-count. | business-rules.md Rule 5 open question | **High** — directly affects whether Phase 3's cost engine over-states costs | Low (not yet traced) |

## Top 5 by severity × likelihood (recommend resolving before Phase 3 locks in calculation logic)

1. **R9** — No version control / reproducibility mechanism (structural, affects the whole product).
2. **R18** — Possible double-counting in COST SUMMARY (affects every job's cost baseline).
3. **R1 / R11** — PRICE OPTIONS broken + two unreconciled margin systems (affects every job's price).
4. **R7** — Broken external link (blocks clean Phase 1 import for any sheet touching it).
5. **R2** — Base!B23 label/value mismatch (small-looking but compounds across every job).

## Explicitly out of scope / not found

- **No malicious macro behavior** (R13) — full VBA decompiled and reviewed, nothing beyond a UI convenience.
- **No intentional circular-reference design** — `calcPr` has no iterative-calculation flag, and no self-referencing formulas were detected (R10), though the COMPONENT-family formula *direction* was not exhaustively traced.
- **No password-protected sheets** — protection exists but is not backed by a password anywhere (R8), so no content was inaccessible to this audit.
