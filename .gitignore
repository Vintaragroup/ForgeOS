# Source workbook — real client/rate data, never goes to the (public) remote.
# The audit tooling expects it at this exact path locally; see docs/audit-plan.md.
/Reference/ORLANDO ESTIMATE.xlsm

# Real employee names to redact from generated artifacts — see
# tools/workbook_audit/redact_local.py's own docstring for why this can't
# be committed either.
/tools/workbook_audit/redact_local.py

# OS
.DS_Store

# Python
__pycache__/
*.pyc
.pytest_cache/

# Editors
.vscode/
.idea/

# Phase 1 generated run output (regenerate with: python3 -m tools.workbook_import.run_phase1)
# Includes the synthetic fixture .xlsm/.xlsx (binary, regenerable) and the
# staging SQLite DB -- not audit deliverables, unlike artifacts/*.json.
/artifacts/phase1_runs/
/artifacts/phase1_staging.db

# Real historical job workbooks -- real client names, rates, and awarded
# prices. Never goes to the (public) remote. See data/historical_jobs/README.md,
# which is the one file in this tree that IS tracked (hence the negation below).
/data/historical_jobs/*
!/data/historical_jobs/README.md

# Real RFP bid documents -- vendor services agreements, financial proposal
# schedules, site plans for actual client events. Same "never goes to the
# public remote" reasoning as data/historical_jobs/ above.
/data/RFP/

# Expo CCI's internal brand identity manual (PDF) -- proprietary company
# material, not something to publish alongside the open app code.
/data/ExpCCI-brandguidelines/

# Ground-truth estimating workbook extraction (real historical materials/
# rental/labor-rate data) and the BeMatrix vendor price list -- same
# "never goes to the public remote" reasoning as the other data/ entries
# above. web/scripts/generate-catalog-expansion.ts reads this locally and
# writes its output to the committed web/prisma/seed-data/ instead.
/data/Catalog_Data/

# The Graphics team's real PGA-show control log -- real client names,
# vendors, and booth-graphic production detail. Same "never goes to the
# public remote" reasoning as the other data/ entries above.
# web/scripts/import-pga-graphics-log.ts reads this locally.
/data/Graphics-operations/

# Real customer list exported from Salesmate (the sales pipeline system) --
# real client names, addresses, and phone numbers. Same "never goes to the
# public remote" reasoning as the other data/ entries above.
# web/scripts/import-salesmate-companies.ts reads this locally.
/data/Sales-mate_customer-list/

.vercel
.env*
data/Screenshot 2026-09-02 at 1.22.38 PM.png
data/H1-header-differences/Screenshot 2026-09-04 at 8.08.57 PM.png
data/H1-header-differences/Screenshot 2026-09-05 at 9.35.47 AM.png
data/H1-header-differences/Screenshot 2026-09-05 at 9.35.59 AM.png
data/H1-header-differences/Screenshot 2026-09-05 at 9.36.06 AM.png
data/H1-header-differences/Screenshot 2026-09-05 at 9.36.10 AM.png
data/H1-header-differences/Screenshot 2026-09-05 at 9.36.16 AM.png
data/H1-header-differences/Screenshot 2026-09-05 at 9.36.27 AM.png
data/H1-header-differences/Screenshot 2026-09-05 at 9.36.31 AM.png
data/Project-Timeline.png
data/Project Info.png
data/Project timline.png
data/Proof-Artwork/Artwork/Fifa2.png
data/Proof-Artwork/Artwork/Fifa3.png
data/Proof-Artwork/Proof_SO399945-1779548495636.pdf
data/Proof-Artwork/Artwork/Fifa1.png
